
#include <stdio.h>
#include <stdlib.h>

int main() {

    // int a;
    // int *p;
    // p = &a;
    // a = 5;
    // printf("%p", p);
    

    // //Pointers and dynamic memory 
    // int a;
    // int* p = new int; //C++ version of malloc()
    // *p = 10;                                             //new and delete are type-safe
    // delete p; //C++ version of free()
    // p = new int[20]; //array heap allocation
    // delete[] p; //array heap deletion

}